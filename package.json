{  
  "name": "followup-signature",  
  "version": "1.0.0",  
  "description": "Google Workspace signature app for marketers and users",  
  "main": "src/index.js",  
  "scripts": {  
    "dev": "next dev",  
    "build": "next build",  
    "start": "next start",  
    "prisma:migrate": "prisma migrate dev",  
    "prisma:generate": "prisma generate"  
  },  
  "author": "gtmunlocked",  
  "license": "MIT",  
  "dependencies": {  
    "@prisma/client": "^5.0.0",  
    "next": "^13.0.0",  
    "react": "^18.0.0",  
    "react-dom": "^18.0.0",  
    "googleapis": "^126.0.0"  
  },  
  "devDependencies": {  
    "prisma": "^5.0.0",  
    "typescript": "^5.0.0"  
  }
}